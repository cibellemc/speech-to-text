# ─────────────────────────────────────────────────────────────────────────────
# Pinned para garantir compatibilidade com torch==2.4.1 + CUDA 12.4
# + pyannote.audio 3.3.2 (usa itertracks e aceita token=)
# ─────────────────────────────────────────────────────────────────────────────

# pyannote — fixado em 3.3.2 (última versão estável com itertracks + token=)
pyannote.audio==3.3.2

# huggingface_hub compatível com token= em Pipeline.from_pretrained
huggingface_hub>=0.23.0,<0.25.0

# Streamlit e utilitários web
streamlit>=1.35.0,<1.40.0
python-docx>=1.1.2
requests>=2.31.0

# Banco de dados
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9

# Matplotlib
matplotlib>=3.8.0

# Whisper (openai-whisper usa torch, não precisa de versão extra)
openai-whisper>=20240930

# Áudio
pydub>=0.25.1

# transformers compatível com pyannote 3.3.2
transformers>=4.40.0,<4.46.0

# numpy < 2 (requerido pelo torch 2.4.x)
numpy<2